name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
    
    - name: Build Solution
      run: |
        msbuild TouchFreeze.sln /p:Configuration=Release /p:Platform=x86
    
    - name: Create Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          Sources/Setup/bin/Release/TouchFreeze.msi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
